version: 2

models:
  - name: customers
    description: one record per customer with fields from orders
    columns:
      - name: customer_id
        description: primary key of model
        tests:
          - unique
          - not_null
